import { SearchservicePipe } from './searchservice.pipe';

describe('SearchservicePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchservicePipe();
    expect(pipe).toBeTruthy();
  });
});
